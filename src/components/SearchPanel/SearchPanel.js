import React from 'react';

import './SearchPanel.css';

const SearchPanel = () => {
  return (
    <input className="form-control search-input" placeholder="поищи важное" />
  );
};

export default SearchPanel;
